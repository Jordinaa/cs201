#pragma once
#include <iostream>
#include <vector>
#include "Person.h"
#include "Student.h"


int readFile(vector<person>, vecto<Student> );
void printVector(vector<person> const, vector<student> const);